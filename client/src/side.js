export function Side(props) {
    return (
        <div className="sideBox">
            <h3 className="side">
                {props.firstName} {props.lastName}
            </h3>
        </div>
    );
}
